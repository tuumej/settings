# description : 
# This file is intended to clarify the concept.

1. Ubuntu's default shell is dash
Checking default shell : vi /etc/passwd or ls -al /bin/sh
# change default shell dash -> bash
sudo dpkg-reconfigure dash
=> Select 'No'
# confirm default shell
ls -al /bin/sh
# Why?

2. 
